let res;
module.exports = {
   set: (set = require('./set')),
   add: (add = require('./add')),
   sub: (sub = require('./sub')),
   div: (div = require('./div')),
   mult: (mult = require('./mult')),
   get: (get = require('./get')),
};
